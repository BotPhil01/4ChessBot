#include "bitboard.h"
#include"engine.h"

#ifndef THREADING_H
#define THREADING_H

// used for spawning multiple threads off 
namespace threading {
    class Master {
        public:
            Master(board &p_board, types::PieceColour p_colour) {

            }
        private:

    };
}


#endif
